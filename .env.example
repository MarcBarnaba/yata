# Supabase cloud sync (optional).
# Leave blank to run GSD in local-only mode (data stays in this browser).
# Create a free project at https://supabase.com, then copy:
#   Project Settings → API → Project URL          -> SUPABASE_URL
#   Project Settings → API → Project API keys → anon/public -> SUPABASE_ANON_KEY
SUPABASE_URL=
SUPABASE_ANON_KEY=
